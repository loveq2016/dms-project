<?xml version="1.0" encoding="UTF-8" ?>  
    <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="OrderDetail">
	<sql id="adsWhere">
		<trim prefix="WHERE" prefixOverrides="AND|OR">
			<if test="@com.yijava.orm.core.OgnlUtils@isNotEmpty(product_name)">
				  and product_name=#{product_name}
			</if>
		</trim>
	</sql>
	<sql id="adsOrder">
		<choose>
		    <when test="@com.yijava.orm.core.OgnlUtils@isNotEmpty(orderSql)">  
		         ORDER BY ${orderSql}
		    </when>
		    <otherwise> 
            </otherwise>
	    </choose>
	</sql>
    <select id="selectByObjectcount" resultType="long">
		select id,order_id,product_item_number,product_name,models,order_number_sum,order_price,order_money_sum,discount,delivery_sum,remark,plan_send_date,last_time from tb_order_detail t
		<include refid="adsWhere"/>
	</select>
 	<select id="selectByObject" resultType="dms.yijava.entity.order.OrderDetail">
		select id,order_id,product_item_number,product_name,models,order_number_sum,order_price,order_money_sum,discount,delivery_sum,remark,plan_send_date,last_time from tb_order_detail t
		<include refid="adsWhere"/>
		<include refid="adsOrder"/>
	    limit #{offset},#{pagesize}
	</select>
    <select id="selectByMap" resultType="dms.yijava.entity.order.OrderDetail">
		select * from dms.tb_order_detail where order_code = #{order_code}
	</select>
	<insert id="insert" parameterType="dms.yijava.entity.order.OrderDetail">     
    	  insert into dms.tb_order_detail(order_id,product_item_number,product_name,models,order_number_sum,order_price,order_money_sum,discount,remark,plan_send_date,last_time)
    	  values(#{order_id},#{product_item_number},#{product_name},#{models},#{order_number_sum},#{order_price},#{},#{order_money_sum},#{discount},#{plan_send_date},NOW())
    </insert>
	<delete id="deleteByPrimaryKey" parameterType="int">
   		 delete from dms.tb_order_detail where product_item_number=#{product_item_number}
    </delete>
</mapper>